type: object
properties:
  status:
    type: integer
    example: 400
  message:
    type: string
    example: "Validation failed"
  data:
    type: array
    items:
      type: string
    example: ["Name must be at least 3 characters long"]